# Servidor

> Só testando mesmo.

Criar um arquivo `.env` com o mesmo formato do `.env.exemplo`.

- Pra instalar os pacotes: `npm install`.
- Pra iniciar: `npm start`.

Pasta de requests pode ser importada no [bruno](https://www.usebruno.com/)

Por padrão o aplicativo roda na porta :3080

### Fazer

- criar uma variável pra base do endereço da api pra evitar tanta repetição
